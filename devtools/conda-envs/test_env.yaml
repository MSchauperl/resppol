name: test
channels:
  - openeye
  - rdkit
  - omnia

dependencies:
    # Base depends
  - python
  - pip
  - time
  - typing
  - scipy
  - numpy
  - openeye-toolkits
  - rdkit
  - openforcefield

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
