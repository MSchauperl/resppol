package:
  name: resppol
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  build:
    - python
    - numpy
    - setuptools

  run:
    - python

test:
  requires:
    - pytest
  imports:
    - resppol

about:
  home: add_url_here
  license: MIT License
