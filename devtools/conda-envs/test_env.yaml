name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - time
  - typing
  - scipy
  - numpy

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
