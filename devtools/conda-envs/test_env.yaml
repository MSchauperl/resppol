name: test
channels:
  - openeye
  - rdkit

dependencies:
    # Base depends
  - python
  - pip
  - time
  - typing
  - scipy
  - numpy
  - openeye-toolkits
  - rdkit

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
